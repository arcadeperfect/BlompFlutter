import 'package:flutter/material.dart';

class ScanFound extends StatelessWidget {
  const ScanFound({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Scan QR'),
      ),
    );
  }
}